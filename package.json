{
  "name": "claude-export",
  "version": "1.0.0",
  "description": "Export Claude conversations from conversations.json to Markdown files",
  "type": "module",
  "bin": {
    "claude-export": "./bin/cli.js"
  },
  "main": "src/index.js",
  "scripts": {
    "test": "node --test test/*.test.js",
    "site:dev": "npm --prefix website run dev",
    "site:build": "npm --prefix website run build",
    "site:preview": "npm --prefix website run preview"
  },
  "keywords": [
    "claude",
    "anthropic",
    "export",
    "conversations",
    "markdown"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "commander": "^12.0.0"
  }
}
